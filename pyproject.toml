[project]
name = "school-finance"
version = "0.1.0"
description = "School finance web app — fee collection, expenses, arrears tracking"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
